# Storage

Слой сохранения состояния расширения.

Что здесь лежит:

- `04-storage.js` — загрузка и сохранение настроек/состояния расширения.

Важно:

- Здесь должна быть только логика чтения и записи состояния.
- UI-обновления и DOM-манипуляции лучше держать вне этой папки.
